get:
  tags: [ Inbound APIs ]
  summary: Get templates
  description: |
    Get templates
  security:
    - crmgAuthorization: []
 
  responses:
    '200':
      description: crmgrow log in successfully
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/TemplateModel'