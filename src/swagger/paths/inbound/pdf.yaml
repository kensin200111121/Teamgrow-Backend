get:
  tags: [ Inbound APIs ]
  summary: Get pdfs
  description: |
    Get pdfs
  security:
    - crmgAuthorization: []
 
  responses:
    '200':
      description: crmgrow log in successfully
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/PdfModel'